import matplotlib.pyplot as plt
import numpy as np


#X, Y = np.meshgrid(np.arange(-5,5,.01),np.arange(-5,5,0.01))
#R = (X**2 + Y**2)**0.5

# cam class with position, field of view


# for each position in discretised space, calculate visibility, range, and triang angle, triang area.


# plot
